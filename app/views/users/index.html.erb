
<h1>User#index</h1>
<p>Find me in app/views/user/index.html.erb</p>

<%= link_to("Create User", new_user_path, class: 'btn btn-primary', id: 'new_link', remote: true) %>
<table class="table">
<thead>
<tr>
  <th scope="col">id</th>
  <th scope="col">First name</th>
  <th scope="col">Last_name</th>
  <th scope="col">Phone</th>
  <th scope="col">Email</th>
</tr>
</thead>
  <div id="tablet1">
  <% @user1.each do |user| %>
  <tr id='<%= user.id %>'>
  <td><%= link_to(user.first_name, user, class: 'link') %></td>
  <td><%= user.last_name %></td>
  <td><%= user.phone %></td>
  <td><%= user.email %></td>
  <td><%= link_to("update", edit_user_path(user), class: 'btn btn-primary btn-lg') %></td>
  <td><%= link_to("delete", delete_user_path(user), class: 'btn btn-primary btn-danger') %></td>
      <td class='destroy'>Destroy</td>
      </tr>

<% end %>

</table>
</div>